# demo-rag-chat

クラウドワークス応募用ポートフォリオの自主制作デモ。
**架空の製品マニュアル・FAQを使った自主制作であり、実案件の納品物ではない**（READMEにも明記済み。外さないこと）。

## 何のデモか

出典付きで回答するRAG（検索拡張生成）チャット。

1. **チャンク分割**（`app/chunking.py`）— Markdownは見出し単位、PDFはページ単位。長文のみ重なり付き分割
2. **検索**（`app/search.py`）— 依存なしの自前BM25。日本語は文字バイグラムでトークン化
3. **回答生成**（`app/rag.py`）— ヒットした抜粋だけを根拠にClaude（Haiku）が回答。出典番号【n】必須、根拠がなければ「確認できない」と答えさせる

「チャンク設計・検索設計・プロンプト設計を自前で実装できる」ことを示すのが目的。
LangChain等のフレームワークやベクトルDBを安易に足さない（デモの主張が薄まる）。

## 設計上の重要な制約

- **ステートレス設計**: アップロードPDFのチャンクはサーバーに保存せずクライアントに返し、
  質問ごとにリクエストへ載せて往復させる。Vercelのサーバーレス（インスタンス間で状態共有不可）前提の
  意図的な設計。サーバー側にセッション保存を足さないこと
- **デモの上限**: PDF 2MB/100ページ、質問500文字、追加チャンク400個、IPごと30回/日（コスト暴走防止。緩めない）
- モデルは **claude-haiku-4-5 固定**（コスト最優先）
- `ANTHROPIC_API_KEY` 未設定なら検索は実動作・回答はモック。ローカル開発はキーなしで行う
- `data/docs/` のドキュメントはすべて架空。実在の製品・企業の文書を入れない

## 開発

```bash
pip install -r requirements.txt
uvicorn app.main:app --reload --port 8124   # http://localhost:8124
python tests/test_rag.py                    # テスト（APIキー不要）
```

## デプロイ

Vercel。`api/index.py` がエントリポイント、`vercel.json` で全パスをrewrite。
環境変数 `ANTHROPIC_API_KEY` は Vercel の Project Settings に設定する。
