# Claude API キー（未設定でも起動可能。その場合は検索のみ実動作し、回答生成はモック）
# Vercel では Project Settings → Environment Variables に設定すること
ANTHROPIC_API_KEY=
# IDリンク型キーの場合は必須（Anthropicコンソールのワークスペース設定に表示されるID）
ANTHROPIC_WORKSPACE_ID=
